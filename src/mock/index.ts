/**
 * Scriptable mock Wyoming server for tests (subpath export
 * `signalk-wyoming/mock`). Consumed as a devDependency by the
 * signalk-piper / signalk-whisper / signalk-openwakeword test suites and by
 * this repo's own protocol/orchestrator tests.
 */

export * from "./server.js";
